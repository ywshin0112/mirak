<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="kr.co.mirak.pay.PayMapper">
 	
 
 	<select id="list" resultType="kr.co.mirak.pay.PayVO">
 		select * from test
 	</select>
 	
 	
 	
  	
  	<insert id="insert" parameterType="kr.co.mirak.pay.PayVO">
 		insert into test (name)
 		values (#{name})
 	</insert>
 	
 	<!-- 카트 목록 -->
	<select id="cartCheckList" resultType="kr.co.mirak.cart.CartVO">
	
		select * from cart where mem_id=#{myid} and cart_check=1
	
	</select>
 	
 	<!-- 
 	<select id="info" resultType="kr.co.mirak.pay.PayVO">
 		select * from emp2 where empno=#{empno}
  	</select>
 	
 	<update id="update" parameterType="org.sp.mybatis.Emp">
 		update emp2 set deptno=#{deptno}, sal=#{sal}
 		where empno=#{empno}
 	</update>
 	
 	<delete id="delete">
 		delete from emp2 where empno=#{empno}
 	</delete>
 	 -->
 </mapper>